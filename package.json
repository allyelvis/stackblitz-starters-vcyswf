{
  "name": "json-server-starter",
  "private": true,
  "scripts": {
    "frontend": "http-server",
    "backend": "json-server -w db.json"
  },
  "dependencies": {
    "http-server": "^0.12.3"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
