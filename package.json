{
  "name": "json-server-starter",
  "private": true,
  "scripts": {
    "frontend": "http-server",
    "backend": "json-server -w db.json"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "json-server": "^0.16.3"
  }
}
